<?php /** @noinspection NonAsciiCharacters */

namespace Finish\LT;


class 「単一責任の原則」の良さを伝えるLT
{

    // LTで何をするかだけを所持している
    public function start()
    {
        $this->「単一責任の原則」が満たされない場合の例を述べる();
        $this->「単一責任の原則」を少しずつ適用し、不安を取り除いていく();
        $this->「単一責任の原則」を適用すると、不安がなくなることを例を用いて伝える();
    }

    // 「単一責任の原則が満たされない場合の例」だけを所持している
    private function 「単一責任の原則」が満たされない場合の例を述べる()
    {
    }

    private function 「単一責任の原則」を少しずつ適用し、不安を取り除いていく()
    {
    }

    private function 「単一責任の原則」を適用すると、不安がなくなることを例を用いて伝える()
    {
    }
}